{
  "name": "mon-site",
  "version": "0.0.0",
  "description": "website!",
  "scripts": {
    "test": "intern-runner config=tests/intern reporters=pretty",
    "start": "cat sig.md && harp server app",
    "build": "harp compile app && echo static files built!"
  },
  "author": "ricky miller caterpillar",
  "devDependencies": {
    "harp": "^0.25.0",
    "intern": "^4.2.0"
  }
}
